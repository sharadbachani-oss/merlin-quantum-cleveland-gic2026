# Cleveland — current package

Read **CLEVELAND_PROPOSAL_v7.md** (source) or **report.pdf** (the Phase-1 concept proposal as submitted, v7.5). These supersede SUBMISSION_NATIVE_v6.md and the historical narratives in legacy_reports/. Run `python verify.py` (numpy only) to replay the headline numbers from archived counts.

Status: HARDWARE_RESPONSE_RECORDED_APPLICATION_AND_MAP_GATES_OPEN

Next: Complete four IBM residue maps with null/coupling controls and independent replication; validate molecular bridge.

Evidence and code are retained. Historical flight scripts are not automatically validated for a new experiment; use NATIVE_EXPERIMENT.json as the current contract. Shared tools are in the parent directory.
